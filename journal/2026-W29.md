# 2026-W29

## Autopilot Repo Conventions Now Written Into SOUL.md (2026-07-13)

**Context.** Commit `66c1daf` (Earnest, 2026-07-11, "Harden LinkedIn drafting
and mirror Telegram approvals") mixed voice-prompt edits
(`prompts/linkedin_post_system.md`) with R code changes in a single commit,
and did not archive the prior prompt version to `prompts/archive/` before
overwriting. Both steps are conventions in the autopilot repo's CLAUDE.md,
but that file addresses Claude Code sessions — Earnest's SOUL.md had no git
conventions at all, so nothing was violated on his side. Gap, not fault.

**Decision.** The binding conventions now live in SOUL.md ("Working in the
Autopilot Repo" section), which Earnest loads from the repo:

1. Archive any `prompts/` file to `prompts/archive/YYYY-MM-DD_<filename>.md`
   before overwriting it.
2. Voice-prompt commits stay separate from R code commits.
3. Date-stamped header comment on any modified R script, most-recent-first.
4. Pull before push (Manfred also ships to main).

**No retroactive fix.** The pre-edit prompt version is recoverable at
autopilot commit `879997a`; the archive convention applies from today
forward. SOUL.md also gained a Content Distribution bullet describing the
LinkedIn approval mirror Earnest built in `66c1daf`, so his instruction file
matches what he actually operates.

**Verification owed.** Earnest to confirm his OpenClaw instance reads
`~/autopilot/SOUL.md` (the repo copy) and not a stale local copy — the
Bundle #1 single-source-of-truth decision (2026-W24) implies it does, but it
has not been explicitly checked for SOUL.md.

## Amendment: Verification Answered — Workspace SOUL, Not Repo SOUL (2026-07-13)

Earnest confirmed he runs `/Users/merrittocracyclaw/.openclaw/workspace/SOUL.md`
(workspace-level, general operating principles), NOT `~/autopilot/SOUL.md`.
The repo SOUL.md was therefore documentation, not loaded instructions — the
W29 convention edit above did not reach him by itself.

**Fix (single source of truth preserved):** one pointer line added to the
workspace SOUL.md — when working in `~/autopilot`, the repo's SOUL.md and
CLAUDE.md are binding and must be read before committing there. Repo-specific
conventions stay in the repo; the workspace file only points. No content is
mirrored between the two files (per the Bundle #1 anti-mirror decision).

**Note:** repo SOUL.md's behavioral rules (approval loop, never-post-
autonomously) were evidently never operative as loaded instructions; actual
enforcement has always been the Sheet approval gate plus OpenClaw's own
config, which is why behavior matched anyway.
