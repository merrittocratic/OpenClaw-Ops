# Week of April 21, 2026

## 2026-04-21

### Secrets Migration: 1Password CLI → macOS Keychain

## Type
- [x] Credential rotation or access change
- [x] Configuration change

## Summary
Replaced 1Password CLI (`op run`) with macOS Keychain for runtime secret injection in launchd jobs. Eliminates dependency on 1Password desktop app on Mac Mini.

## Details
- Created `scripts/autopilot-env.sh`: wrapper script that reads secrets from macOS Keychain (service: `autopilot`), exports as env vars, then `exec`s the command
- Created `scripts/keychain-sync.sh`: utility for populating/updating/verifying Keychain entries (interactive, single-secret, and `--verify` modes)
- Updated both launchd plists to call `autopilot-env.sh` instead of `op run --env-file=.env.template`
- Fixed double `op run` bug in `com.merrittocratic.autopilot.post.plist`
- Removed `EnvironmentVariables` block from plists (now handled by wrapper)
- 12 secrets migrated to Keychain, all verified ✅
- Commits: `4690196`, `c069ada`, `6ab8420`

## Why
`op run` requires the 1Password desktop app running with CLI integration enabled. In launchd context, there's no interactive session to authenticate. Additionally, giving the Mac Mini access to the full 1Password account (including Personal vault) was an unnecessary security exposure. Keychain secrets are scoped to the `autopilot` service and encrypted at rest via the login keychain.

## Verification
- `./scripts/keychain-sync.sh --verify` — all 12 secrets confirmed ✅
- `./scripts/autopilot-env.sh /usr/bin/env` — all 14 env vars (12 secrets + REVIEW_SHEET_ID + R_LIBS_USER) injecting correctly
- Full `--check` run completed end-to-end: 10 articles processed, 48 tweets drafted and written to Google Sheets

## Rollback / Kill Switch
- Revert plists to previous versions (restore `op run` in ProgramArguments)
- Keychain entries can be deleted: `security delete-generic-password -s autopilot -a <KEY_NAME>`
- Previous plist versions in git history

## Follow-ups
- [ ] Note: `security unlock-keychain` required when SSH'd in before running `keychain-sync.sh`
- [ ] 1Password app can be uninstalled from Mac Mini (no longer needed at runtime)

---

### Google Sheets Service Account Auth

## Type
- [x] Credential rotation or access change
- [x] Configuration change

## Summary
Replaced interactive OAuth browser flow with Google Cloud service account for headless `googlesheets4` auth in R pipeline.

## Details
- Created Google Cloud project: `merrittocracy-autopilot`
- Service account: `autopilot-sheets@merrittocracy-autopilot.iam.gserviceaccount.com`
- JSON key stored at `~/autopilot/.secrets/google-service-account.json` (chmod 600, gitignored via `.secrets/`)
- Auth call added to `R/00_config.R`: `gs4_auth(path = GOOGLE_SA_KEY)` at load time
- Removed interactive `gs4_auth(scopes = ...)` from `R/04_queue_write.R`
- Google Sheet shared with service account email (Editor access)
- Commit: `c069ada`

## Why
`googlesheets4` interactive OAuth requires a browser, which isn't available in launchd context. Service accounts authenticate via JSON key file — no browser, no session, works headlessly.

## Verification
- `--status` run: successfully read queue (21 pending, 10 posted)
- `--check` run: successfully appended 48 new rows to queue

## Rollback / Kill Switch
- Remove `gs4_auth(path = ...)` block from `R/00_config.R`
- Restore `gs4_auth(scopes = ...)` in `R/04_queue_write.R`
- Revoke service account access in Google Cloud Console

## Follow-ups
- [ ] Service account key has no expiration — consider rotation schedule

---

### Feed Check Interval: 15 → 30 Minutes

## Type
- [x] Scheduled task added or modified

## Summary
Reduced RSS feed polling frequency from every 15 minutes to every 30 minutes.

## Details
- Updated `StartInterval` in `com.merrittocratic.autopilot.check.plist` from 900 to 1800
- Plist reloaded via `launchctl unload/load`
- Commit: `6ab8420`

## Why
15-minute polling was unnecessary — Steve publishes infrequently, and the marginal speed improvement didn't justify double the API calls (Claude drafts, Google Sheets writes).

## Verification
- `launchctl list | grep merrittocratic` — job loaded with exit code 0

## Rollback / Kill Switch
- Change `StartInterval` back to 900 in plist, reload

---

### .env.template Vault Migration (by Steve)

## Type
- [x] Configuration change

## Summary
All `op://` URIs in `.env.template` migrated from `Personal` vault to dedicated `Merrittocracy Agent` vault. Added `GITHUB_TOKEN` entry.

## Details
- 11 existing secrets moved from `op://Personal/...` to `op://Merrittocracy Agent/...`
- New entry: `GITHUB_TOKEN=op://Merrittocracy Agent/GitHub PAT - merrittocratic MacMini/credential`
- Header updated to explicitly prohibit `op inject` (no static .env files)
- Commit: `0b41957` (by Steve)

## Why
Vault isolation — project secrets separated from personal secrets. The `.env.template` remains the canonical reference for what secrets the pipeline needs, even though runtime injection now uses Keychain.

## Verification
- Template reviewed and confirmed correct

---

### Pending: Telegram Approval Notifications

## Type
- [x] Other: planned feature, blocked

## Summary
Cron job to notify Steve via Telegram when new draft threads are pending review. Blocked on OpenClaw gateway cron auth issue.

## Details
- Design: isolated cron job runs every 30 min (6am–9pm ET), checks Google Sheet for new pending rows, sends Telegram summary for approval
- Gateway returns `pairing required` when cron tool attempts to connect
- Gateway config has token auth mode; cron tool may not be passing the token

## Follow-ups
- [ ] Debug gateway pairing issue for cron tool
- [ ] Set up Telegram approval flow (approve/reject/edit via reply)
- [ ] Phase 2: X monitoring for reply opportunities (after thread voice calibration)
