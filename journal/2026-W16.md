# Week of April 14, 2026

## 2026-04-19

### Pre-Launch Secrets Management Setup
- Upgraded 1Password 7 → 8, CLI integration enabled via Settings → Developer
- API keys stored as API Credential items (username = env var name)
- .env.template pattern established in autopilot repo
- Verification: `op read "op://Personal/Anthropic API Key/credential"` returned key value

### GitHub Fine-Grained PAT
- Created PAT scoped to OpenClaw-Ops and autopilot repos, Contents read/write only
- Stored in 1Password as GITHUB_TOKEN, 90-day expiration
- Verification: token created successfully, saved to 1Password

### Telegram + Bot Setup
- Personal account created: @themerrittocracy
- merrittocracy_bot created via BotFather, token stored in 1Password

### Proton Mail
- themerrittocracy@proton.me created as dedicated agent email identity

### Mac Mini Initial Setup
- Remote Login enabled, break-glass SSH confirmed
- Touch ID configured, Wallet disabled, iCloud Keychain skipped
- Verification: `ssh stephenmerritt@Stephens-mac-mini.local` connected successfully
- Homebrew/Node/1Password/OpenClaw are installed 
- Follow-up: create dedicated OpenClaw OS user (checklist Step 1)

## 2026-04-20

### Mac Mini Setup — Continued (merrittocracyclaw user)
- Created dedicated OS user: merrittocracyclaw (Standard, no Apple ID, 
  no iCloud)
- Added merrittocracyclaw to Remote Login allowed users
- Granted Homebrew write permissions to merrittocracyclaw via admin chown
- Installed R (headless, no Positron — Mini is factory floor not workshop)
- Cloned autopilot and OpenClaw-Ops repos as merrittocracyclaw
- Verified git access via GitHub fine-grained PAT

### 1Password CLI on merrittocracyclaw
- Resolved desktop app dependency — claw user authenticates via 
  `op account add` + `eval $(op signin)` directly
- Decided: secrets injection happens at admin level via `op run` wrapper 
  at OpenClaw startup — claw user does not need 1Password app installed
- Follow-up: implement `op run` wrapper for launchd service startup

### OpenClaw Onboard
- Ran `openclaw onboard` as merrittocracyclaw — Quickstart path
- Auth: Anthropic API key
- Search: Gemini (reuses existing API key, no new account)
- Hooks enabled: boot-md, command logger, session memory
- Skills: skipped (vet before installing per security checklist)
- Gateway token saved to 1Password as OPENCLAW_GATEWAY_TOKEN
- Verification: gateway running, connected | idle confirmed in TUI

### Telegram Channel Connected
- merrittocracy_bot paired to OpenClaw via 
  `openclaw pairing approve telegram 8ZZVTGE4`
- Agent responding in Telegram — channel confirmed working
- Agent named: Earnest

### SOUL.md
- Drafted and committed to autopilot repo
- Voice section grounded in published posts (origin post + Simpson/Pickett)
- Defines identity, approval loop, operating principles, what success 
  looks like

### Deferred to Next Session
- Implement `op run` wrapper (replaces static .env approach)
- Fix launchd paths (steve → merrittocracyclaw, R 4.4 → 4.5)
- Backfill existing Substack posts in pipeline
- Google Sheets OAuth browser flow
- Install and start launchd jobs

