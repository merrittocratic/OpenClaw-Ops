# Week of June 8, 2026

## 2026-06-13

### Tier-Aware X Monitor + Single Source of Truth Migration

## Type
- [x] Configuration change
- [x] Scheduled task added or modified
- [x] Other: surfacing logic redesign

## Summary
Redesigned x-monitor surfacing logic around per-account tier metadata (1A/1B/1C/2) with substantiveness, freshness, and engagement-velocity gating. Collapsed the deprecated `~/.openclaw/workspace/scripts/` mirror into a single source of truth under `~/autopilot/openclaw-scripts/`.

## Details

### Autopilot commit
- Commit: `1b4fff3` on `main` (autopilot repo)
- 13 files changed, +674 / -186 lines
- Changes:
  - `openclaw-scripts/x-monitor.R` — tier-aware `score_tweet()`, new helpers (`is_substantive`, `minutes_since`, `compute_engagement_velocity`, `has_analytical_hook`, `derive_draft_style`), new `parse_monitor_list_with_tiers()`, per-tier `TIER_CONFIG`, `COWHERD_HANDLES` constant, `--tier` CLI flag for the fast-lane cron, new candidate JSON fields (`tier`, `is_cowherd`, `minutes_old`, `engagement_per_min`, `is_substantive`, `draft_style`, `draft_prompt_path`), `MAX_CANDIDATES_PER_DAY` soft cap (8/day) separate from `MAX_REPLIES_PER_DAY` (3/day, API only).
  - `openclaw-scripts/x-monitor-list.md` — added `[tier:X]` annotations. Tier-1A: @ColinCowherd, @TheHerd, @ZachLowe_NBA, @Dane_Brugler. Tier-1B: @SharpFootball, @DanielJeremiah, @PFF, @NoLayingUp, @TheRinger (new), @TheRingerNFL (new). Tier-1C: @AdamSchefter, @ShamsCharania, @wojespn, @RapSheet. Tier-2 additions: @BillSimmons.
  - `openclaw-scripts/x-monitor.sh` — invokes `~/autopilot/openclaw-scripts/x-monitor.R` directly (was `~/.openclaw/workspace/scripts/x-monitor.R`).
  - `openclaw-scripts/keyword-sync.sh` — `MONITOR_SCRIPT` points at autopilot canonical.
  - `openclaw-scripts/update-monitor-keywords.sh` — edits autopilot canonical directly, dropped the cp-back-and-push block (now redundant).
  - `openclaw-scripts/x-engaged-accounts.md` — new placeholder; Steve to populate by copying the prior Mac Mini workspace copy on next pull.
  - `prompts/x_reply_tier_{1a, 1a_cowherd, 1b, 1c, 2}.md` — 5 new prompt templates, voice rules locked (first person, no Merrittocracy/Substack/article self-refs, no emojis, no hedging, symmetric comparisons, 240-char ceiling).
  - `openclaw-scripts/README.md` — documents the single-source model and the new prompt-template flow.

### Architecture decisions baked in
- Article keywords still drive tier-2 scoring (preserves existing relevance signal) but tier-1A/1B/1C drafts no longer "attach the article" — drafts stand on their own.
- Cowherd handles (@ColinCowherd, @TheHerd) get a dedicated prompt template tuned for mainstream-talk-radio audience and the longer-game producer-screenshot goal.
- Tier-1C (Schefter/Shams/Woj/Rapoport) requires an analytical hook (model data or article-keyword overlap) — most newsbreaks won't surface, which is intentional.
- Engagement velocity (likes+RTs per minute since posting) is a secondary surfacing signal that overrides keyword thresholds when a tweet is taking off.
- Daily caps: 8 candidates surfaced total, 3 API replies max.

## Why
1. Old scoring required >=2 article-keyword matches across all accounts. A Schefter scoop with 500K impressions and zero article-keyword matches got skipped; a 200-impression tweet that happened to mention "boom rate" got surfaced. Backwards for the visibility-growth goal.
2. The `ARTICLE_TOPICS` keyword soup has grown to 24 entries with heavy overlap. Topical-precision signal is decaying. Tier-1 accounts deserve a different bar entirely.
3. Cowherd is a stretch-goal target (Steve wants to be a guest on his show within 2 years). Consistent data-backed engagement requires a dedicated voice template, not generic article-anchored replies.
4. The dual-mirror setup (`~/.openclaw/workspace/scripts/` and `~/autopilot/openclaw-scripts/`) created drift risk. Single source of truth removes that.

## Earnest's required actions on Mac Mini (in order)
1. `cd ~/autopilot && git pull` to pick up commit `1b4fff3`.
2. Copy the existing engaged-accounts list to the new canonical path so we don't lose manual engagement history: `cp ~/.openclaw/workspace/x-engaged-accounts.md ~/autopilot/openclaw-scripts/x-engaged-accounts.md` (then `cd ~/autopilot && git add openclaw-scripts/x-engaged-accounts.md && git commit -m "data: seed x-engaged-accounts from Mac Mini workspace" && git push`).
3. Update the `openclaw cron` job for x-monitor to call `~/autopilot/openclaw-scripts/x-monitor.sh` instead of `~/.openclaw/workspace/scripts/x-monitor.sh`. Current cron ID: `20f6cc12-7cea-4086-b1eb-22a6e55df1cd` (per W17 entry on 2026-04-25). Cron expression `0 5,8,11,14,17,20 * * *` America/New_York can stay for now — fast-lane cadence comes in bundle #2.
4. Wire the cron-driven drafting step to read `draft_style` + `draft_prompt_path` from each candidate JSON object and use that prompt template (in `~/autopilot/prompts/`) as the system prompt for the reply draft, instead of the prior article-anchored prompt.
5. Once verified: delete the deprecated mirror copies: `~/.openclaw/workspace/scripts/x-monitor.R`, `x-monitor.sh`, `x-monitor-list.md`. Leave the state file `~/.openclaw/workspace/memory/x-monitor-state.json` in place (still the canonical runtime state path).

## Verification (after Earnest completes the above)
- `bash ~/autopilot/openclaw-scripts/x-monitor.sh --hours 3` returns a JSON array (possibly empty) with new fields present: `tier`, `draft_style`, `draft_prompt_path`, `engagement_per_min`.
- `Rscript -e 'parse("~/autopilot/openclaw-scripts/x-monitor.R"); cat("OK\n")'` returns PARSE OK. (Confirmed locally on the laptop pre-commit.)
- At least one full cron-driven scan completes with a candidate that includes a non-empty `draft_prompt_path` pointing at a file that exists on disk.

## Rollback / Kill Switch
- Revert commit `1b4fff3` on autopilot: `git revert 1b4fff3 && git push`.
- Re-point the `openclaw cron` job back at `~/.openclaw/workspace/scripts/x-monitor.sh` (the mirror copies still exist until step 5 of Earnest's actions).
- The keyword-extraction script (`update-monitor-keywords.sh`) targets the autopilot canonical now; on revert it would still attempt to edit autopilot directly, which is fine because the old workspace copies are deprecated but functional. No data-loss risk.

## Follow-ups
- [ ] Bundle #2 (asymmetric polling cadence): create fast-lane cron for tier-1A/1C every 30 min; keep tier-2 on existing 3-hour cadence. Use the new `--tier` flag.
- [ ] Bundle #3 (feedback bucketing): Telegram inline buttons for "Skip / Posted as-is / Posted with edits" feeding a small state log so we can measure tier-by-tier hit rate.
- [ ] Bundle #4 (original-post candidates): bolt onto `morning-sports-digest.R` as a "Today's takes" section with 3-5 stories Steve could write on.
- [ ] After 2-3 weeks of cron-driven runs with the new tiers, audit candidate volume vs. the 8/day soft cap and tune `TIER_CONFIG` thresholds based on actual hit-rate data.

---

### Heartbeat Architecture Deprecated; Cron-Only is the Standard

## Type
- [x] Configuration change
- [x] Other: architectural correction / documentation

## Summary
Documentation cleanup: the heartbeat-based Google Sheet polling and reply-drafting flow described in the 2026-W17 entry on 2026-04-21 ("Pending: Telegram Approval Notifications -> RESOLVED") is no longer in use. All recurring Mac Mini work runs via `openclaw cron` jobs. This entry exists to make that explicit and supersedes the heartbeat references in the W17 entry.

## Details
- Removed stale "heartbeat" references from in-tree comments in `~/autopilot/openclaw-scripts/`:
  - `x-monitor.sh` — header comment now says "Called by an OpenClaw cron job on the Mac Mini."
  - `check-pending-drafts.sh` — same.
  - `README.md` — describes the cron-driven drafting step instead of a heartbeat selector.
- All four edits ride in autopilot commit `1b4fff3`.
- The 2026-W17 entry on 2026-04-21 ("Pending: Telegram Approval Notifications -> RESOLVED") describes the heartbeat-based polling that was the production model at the time. Per the no-retroactive-edit rule, that entry remains as historical record. This W24 entry is the current source of truth on the polling architecture.

## Why
The heartbeat-vs-cron distinction matters for anyone reading the docs to understand how the Mac Mini schedules work. Mixing the two terms in the same repo invites confusion and bad assumptions during incident response.

## Verification
- `grep -rn -i "heartbeat" ~/autopilot/openclaw-scripts/ ~/autopilot/prompts/` returns no matches (confirmed pre-commit).
- `grep -rn -i "heartbeat" ~/autopilot/` (broader sweep) returns no matches.

## Rollback / Kill Switch
- N/A. This is a documentation correction. To revert, restore the prior comment text in the three files via `git revert 1b4fff3`.

## Follow-ups
- [ ] If/when a heartbeat-style loop is reintroduced for any reason, document that decision as a fresh journal entry that references this one.

---

### Asymmetric Polling Cadence: Fast Lane + Slow Lane (Bundle #2)

## Type
- [x] Scheduled task added or modified
- [x] Configuration change

## Summary
Splits the single x-monitor cron job into a fast lane (tier 1A/1B/1C, every 30 min during waking hours) and a slow lane (tier 2, every 3 hours on the existing schedule). Freshness is the strongest reply-engagement signal in our hit-rate analysis, and the old 3-hour cadence let tier-1 tweets age out of their reply window before surfacing.

## Details

### Autopilot commit (prereq)
- `x-monitor.R` `--tier` flag now accepts a comma-separated set
  (`--tier 1A,1B,1C`) in addition to a single tier (`--tier 1A`).
- Single-tier behavior is unchanged; the comma form is additive.
- Parse-tested locally on laptop pre-commit.

### Old cron job to retire
- Job ID: `20f6cc12-7cea-4086-b1eb-22a6e55df1cd` (per W17, 2026-04-25)
- Schedule: `0 5,8,11,14,17,20 * * *` America/New_York
- Command: previously called `~/.openclaw/workspace/scripts/x-monitor.sh`,
  updated in the W24 first entry to `~/autopilot/openclaw-scripts/x-monitor.sh`.
- Action: **delete** after the two new jobs below are running.

### New cron job: x-monitor-fast (tier 1A + 1B + 1C)
- Schedule: `0,30 5-22 * * *` America/New_York
- Frequency: every 30 min, 5:00 AM through 10:30 PM ET (36 runs/day).
- Command: `cd ~/autopilot && bash openclaw-scripts/x-monitor.sh --tier 1A,1B,1C --hours 1`
- Accounts covered: the 14 tier-1 handles (4 + 6 + 4).
- Lookback: 1 hour matches the 30-min cadence — fresh tweets only,
  generous overlap as a missed-poll safety buffer.

### New cron job: x-monitor-slow (tier 2)
- Schedule: `0 5,8,11,14,17,20 * * *` America/New_York (existing slow schedule preserved)
- Frequency: 6 runs/day, 5 AM through 8 PM ET.
- Command: `cd ~/autopilot && bash openclaw-scripts/x-monitor.sh --tier 2 --hours 3`
- Accounts covered: all monitor-list handles without a `[tier:1X]`
  annotation (~28 accounts).
- Lookback: 3 hours matches the 3-hour cadence.

## Why
1. Freshness drives reply engagement. Steve's own hit-rate read on the prior system: ~15-20% surfaced candidates engaged with, but "when I reply in a timely fashion when the post is very new, I get very good engagement. When it is stale, the engagement is worse."
2. The 3-hour cadence meant a Schefter scoop posted at 8:05 AM didn't surface until 11:00 AM, by which point the reply window had closed. Tier-1 accounts deserve a polling cadence that respects this.
3. Tier-2 accounts don't benefit from the same cadence — most surfacings tied to article keyword overlap, which isn't time-sensitive on the same scale.
4. Splitting lanes lets us scale costs predictably: fast lane is 14 accounts * 36 runs/day; slow lane is ~28 accounts * 6 runs/day.

## Cost note
Rough estimate: 3-5x current API spend (which was running ~$5-15/month per W17 entry), so likely $30-50/month total. Steve approved this spend ceiling during scoping. Validate against actual day-1 spend on the X dev account and tune `--hours` / cadence if it materially overshoots.

## Earnest's required actions on Mac Mini (in order)
1. `cd ~/autopilot && git pull` to pick up the comma-separated `--tier` change.
2. Confirm the new flag works: `bash ~/autopilot/openclaw-scripts/x-monitor.sh --tier 1A,1B,1C --hours 1` returns a JSON array (possibly empty) with no errors.
3. Create the two new cron jobs (fast lane + slow lane) with the schedules and commands above. Use the same openclaw cron CLI flow as the original job.
4. Verify both new jobs appear in `openclaw cron list` with the correct schedules.
5. Wait for one fast-lane firing and one slow-lane firing to confirm each lane runs cleanly. Inspect the JSON output: fast lane should only emit candidates with `tier` in {"1A","1B","1C"}; slow lane should only emit `tier == "2"`.
6. Delete the old job (`20f6cc12-7cea-4086-b1eb-22a6e55df1cd`) once both new jobs have run successfully.

## Verification
- `openclaw cron list` shows three x-monitor jobs during transition (old + two new), reduces to two after step 6.
- Manual run: `bash ~/autopilot/openclaw-scripts/x-monitor.sh --tier 1A,1B,1C` produces only tier-1 candidates.
- Manual run: `bash ~/autopilot/openclaw-scripts/x-monitor.sh --tier 2` produces only tier-2 candidates.
- One week of actual cron runs without API rate-limit errors or runaway cost.

## Rollback / Kill Switch
- Delete the two new cron jobs.
- Re-create the original job at the prior schedule (`0 5,8,11,14,17,20 * * *`) with command `cd ~/autopilot && bash openclaw-scripts/x-monitor.sh` (no `--tier` flag, restores all-tier scanning at 3-hour cadence).
- The R code change is backward compatible — no autopilot revert needed.

## Follow-ups
- [ ] After one week of fast-lane operation, audit actual API spend vs. the $30-50/month estimate.
- [ ] Once Bundle #3 (feedback bucketing) lands, measure per-tier hit rate across the fast-lane vs slow-lane split to validate the architecture.
- [ ] Revisit lookback windows (`--hours 1` for fast, `--hours 3` for slow) once we see real overlap-vs-miss data.

---

### Feedback Bucketing via Telegram Inline Buttons (Bundle #3)

## Type
- [x] Feature added
- [x] Scheduled task added or modified

## Summary
Adds three inline feedback buttons (Skip / As-is / Edited) to every x-monitor candidate that gets sent to Telegram. A new cron-driven poller captures the button clicks and appends them to a JSONL feedback log, joinable to the surfacing log by `tweet_id`. After a few weeks of data we can measure per-account hit rate, per-tier edit rate, and time-of-day patterns to validate (or refute) the freshness hypothesis and the tier-architecture decisions.

## Details

### Autopilot changes
- New file: `openclaw-scripts/x-feedback-send.R` -- reads `{"candidate": {...}, "draft": "..."}` from stdin, formats a Telegram message, attaches an `inline_keyboard` with three buttons whose `callback_data` is `fb:{tweet_id}:{bucket_code}` (s/c/e), and appends a record to `data/x-monitor-surfacings.jsonl`. Emits `{"ok": true, "message_id": N}` on success.
- New file: `openclaw-scripts/x-feedback-send.sh` -- env-injected wrapper around the R script.
- New file: `openclaw-scripts/x-feedback-poll.R` -- calls Telegram `getUpdates` with offset tracking, processes any `callback_query` events whose data starts with `fb:`, appends to `data/x-monitor-feedback.jsonl`, and calls `answerCallbackQuery` so the Telegram button clears its loading spinner.
- New file: `openclaw-scripts/x-feedback-poll.sh` -- env-injected wrapper around the R script.
- Updated `.gitignore` to exclude the three new state files (`data/x-monitor-surfacings.jsonl`, `data/x-monitor-feedback.jsonl`, `data/x-feedback-poller-state.json`).
- Updated `openclaw-scripts/README.md` to document both scripts and the new state-file locations.

### Schema

`data/x-monitor-surfacings.jsonl` -- one JSON object per line, written each time `x-feedback-send.R` ships a candidate:
```
{
  "timestamp_utc": "2026-06-13T19:42:11Z",
  "tweet_id":      "1234567890123456789",
  "username":      "ColinCowherd",
  "tier":          "1A",
  "is_cowherd":    true,
  "draft_style":   "tier_1a_cowherd",
  "draft_prompt_path": "prompts/x_reply_tier_1a_cowherd.md",
  "reason":            "velocity_override",
  "engagement_status": "cold",
  "can_reply_via_api": false,
  "minutes_old":         12.4,
  "engagement_per_min":  247.3,
  "relevance_score":     1,
  "prospect_match":      false,
  "draft":               "...the drafted reply...",
  "message_id":          7891
}
```

`data/x-monitor-feedback.jsonl` -- one JSON object per line, written each time `x-feedback-poll.R` ingests a callback:
```
{
  "timestamp_utc": "2026-06-13T19:43:55Z",
  "tweet_id":      "1234567890123456789",
  "bucket":        "posted_edited",
  "bucket_code":   "e",
  "update_id":     94837284,
  "callback_id":   "94837284:1",
  "message_id":    7891,
  "chat_id":       "8676616323",
  "from_user":     "merrittocratic"
}
```

Join the two logs on `tweet_id` (and optionally `message_id`) to compute per-account, per-tier, time-of-day hit rates.

### Cowherd-style choice
Plain text in the Telegram message (no `parse_mode`). Tweet text and drafts can contain Markdown landmines (`*`, `_`, backticks) that would otherwise need defensive escaping. The existing `R/00_config.R` `notify_telegram` already learned this lesson the hard way; the new send script avoids the problem entirely.

## Why
Steve's earlier feedback explicitly named the gap:

> "The manual copy/pasting does occur, but I don't think the feedback mechanism is right to know when I am actually posting/replying with [what he] suggests or post/replying with edits. Feels like we need to bucket that a little. 'Not worth replying/posting', 'Replied/posted as is', 'Replied/posted, but with edits'."

Without this loop, we have no signal on whether the tier-architecture and prompt-template changes from Bundles #1 and #2 are actually moving the needle. Three buckets give us:

| Bucket | Signal |
| --- | --- |
| Skip | Surfacing was wrong (stale, off-topic, bad account) |
| As-is | Both surfacing AND draft worked |
| Edited | Surfacing worked, draft needs tuning |

After a few weeks of logs, per-account skip rates flag tier-2 handles worth pruning, per-tier edit rates flag prompt templates worth iterating, and time-of-day patterns confirm or refute the freshness hypothesis baked into Bundle #2.

## Earnest's required actions on Mac Mini (in order)
1. `cd ~/autopilot && git pull` to pick up the four new scripts.
2. **Wire the drafting step to call `x-feedback-send.sh`** instead of sending plain Telegram messages. After the cron-driven flow generates the draft reply from the tier-specific prompt template, pipe a JSON document of the form `{"candidate": {...candidate JSON from x-monitor.R...}, "draft": "...drafted reply text..."}` to `bash ~/autopilot/openclaw-scripts/x-feedback-send.sh` on stdin. Check the exit status; non-zero means the Telegram send failed and the surfacing was NOT logged.
3. **Create a new cron job: `x-feedback-poll`** to process button clicks.
   - Schedule: `* 5-22 * * *` America/New_York (every minute, 5 AM through 10:59 PM ET).
   - Command: `cd ~/autopilot && bash openclaw-scripts/x-feedback-poll.sh`
   - Frequency rationale: Telegram requires answering a `callback_query` within ~30 seconds or the button shows a loading spinner indefinitely. 1-minute cron keeps click-to-ACK latency under 60 seconds, acceptable UX.
4. **Verify the loop end-to-end**:
   - Trigger a fast-lane surfacing manually if needed: `bash ~/autopilot/openclaw-scripts/x-monitor.sh --tier 1A,1B,1C --hours 24` and pipe the first candidate through the send script.
   - Click each of the three buttons on the Telegram message.
   - Within 1 minute, each click should produce a "Logged: ..." toast in Telegram.
   - Confirm three lines appended to `data/x-monitor-feedback.jsonl`, one per click.

## Cost
- Telegram bot API is free; no incremental spend.
- The X API spend stays where Bundle #2 left it.

## Verification (after Earnest completes the above)
- `bash ~/autopilot/openclaw-scripts/x-feedback-poll.sh` with no pending updates exits cleanly and prints `[x-feedback-poll] no updates`.
- A complete round trip (send -> click -> log -> ACK toast) takes under 60 seconds end-to-end.
- After 24 hours of normal operation, both JSONL files exist and contain valid one-record-per-line JSON.

## Rollback / Kill Switch
- Delete the `x-feedback-poll` cron job. Surfacings continue to be logged but feedback is not captured; buttons in Telegram remain functional (click does nothing visible until ACK times out).
- To also roll back the send side, revert Earnest's drafting step to call the prior plain-Telegram send function instead of `x-feedback-send.sh`. The R code does not need to be reverted -- the scripts can sit idle.

## Follow-ups
- [ ] After two weeks of feedback data, draft a small analysis script (`R/07_feedback_analysis.R` or similar) to summarize per-account, per-tier, time-of-day stats. Out of scope for Bundle #3 itself.
- [ ] If Bundle #4 (original-post candidates) lands, decide whether those also get feedback buttons (likely yes, same three buckets fit).
- [ ] Optional UX polish: after a button is clicked, edit the original Telegram message to strike through the candidate and append `[Logged: <bucket>]`. Currently the toast is the only visible feedback.

---

### Bundle #2 Amendment: Retire Four Additional Legacy x-monitor Cron Jobs

## Type
- [x] Scheduled task added or modified

## Summary
Amends the Bundle #2 entry above. During Earnest's Bundle #2 deploy, four additional all-tier x-monitor cron jobs surfaced beyond the single one (`20f6cc12-...`) named in the original Bundle #2 entry. With the new fast + slow lanes covering the full tier x day-of-week matrix natively, all four are redundant and get retired. The original Bundle #2 entry remains accurate for the jobs it called out; this addendum extends the retirement list.

## Jobs retired in this amendment
- `x-monitor-noon` -- schedule `30 12 * * 1-5`, weekday-only 12:30 PM, no `--tier` flag (all tiers).
- `x-monitor-evening` -- schedule `0 19 * * 1-5`, weekday-only 7:00 PM, no `--tier` flag.
- `x-monitor-weekend` -- schedule `0 8,11,14,17,20,23 * * 0,6`, weekend-only six fires, no `--tier` flag.
- `x-monitor-weekend-overnight` -- schedule `0 5 * * 0,6`, weekend-only 5:00 AM, no `--tier` flag.

These four jobs were added some time after the W17 entry (2026-04-25) to patch weekend and off-hour gaps in the old single-job schedule. The Bundle #2 fast + slow lanes both run 7 days a week (no day-of-week filter), so the gap-patching is no longer needed.

## Why retire
1. **Redundant coverage.** Every time slot these four hit is already hit by fast lane (tier 1) or slow lane (tier 2), with the single exception called out below.
2. **Wasted API spend.** Each duplicate fetch is a billable X API read with no new candidate signal -- the `seen_tweet_ids` dedup in `x-monitor.R` prevents re-surfacing the same tweet, so the extra reads buy nothing.
3. **State-file write race.** Multiple jobs firing in the same minute write to `~/.openclaw/workspace/memory/x-monitor-state.json` concurrently. Race risks: lost `seen_tweet_ids` (re-surfacing the same tweet) or lost `daily_candidate_count` (overshooting the 8/day cap).

## Coverage gap acknowledged
The only time slot lost by retiring all four is **weekend 11:00 PM ET** (the `x-monitor-weekend` 23:00 fire). Fast lane currently ends at 22:30 (`0,30 5-22 * * *`). Sports news is quiet overnight, so the gap is accepted as a deliberate tradeoff. If real-world reply-engagement data ever shows weekend late-night opportunities being missed, the fix is a one-line cron amendment to extend the fast lane to `0,30 5-23 * * *` (38 fires/day vs 36) -- no code changes needed.

## Earnest's required actions
1. Delete the four cron jobs above via `openclaw cron delete`.
2. After deletion, `openclaw cron list` should show exactly three x-monitor-related jobs:
   - `x-monitor-fast` (Bundle #2)
   - `x-monitor-slow` (Bundle #2)
   - `x-feedback-poll` (Bundle #3)

## Verification
- `openclaw cron list | grep x-monitor` returns exactly the three expected jobs.
- After 24 hours, no concurrent-write artifacts visible in `~/.openclaw/workspace/memory/x-monitor-state.json` (JSON parses cleanly, `seen_tweet_ids` array length grows monotonically, `daily_candidate_count` does not exceed 8 within a calendar day).

## Rollback / Kill Switch
- Restore any of the four jobs by re-creating them with the schedules captured above and command `cd ~/autopilot && bash openclaw-scripts/x-monitor.sh` (no `--tier` flag). Not recommended unless a specific coverage gap is identified.

## Follow-ups
- [ ] If weekend 11 PM ET turns out to matter after Bundle #3 feedback data lands, extend fast lane to `0,30 5-23 * * *` and document in a new journal entry.

---

### Today's Takes Bolted Onto morning-sports-digest (Bundle #4)

## Type
- [x] Feature added

## Summary
Adds an original-post candidate channel to `morning-sports-digest.R`. After the existing digest send, a second Claude Haiku call runs against the same tweet bundle to produce 3-5 "Today's Takes" -- stories Steve could write a Substack post or original X thread on, each with trigger / angle / hook. Sent as a separate Telegram message so Steve can review in a different mental mode (browse-for-writing vs. action-on-reply).

## Details

### Autopilot changes
- Edited `openclaw-scripts/morning-sports-digest.R`:
  - Added `call_claude_takes(tweet_bundle, date_label)`: second Claude Haiku call with a takes-specific prompt that biases toward stories with a data angle in Steve's editorial lanes (NFL draft analytics, NBA playoffs, golf majors, narrative-vs-data takes).
  - Wired the takes call into the main flow immediately after the digest send. Failures are isolated -- if takes fail, the digest still went out and vice versa.
  - When yesterday's news doesn't rise to take-worthy, the takes prompt returns the literal sentinel `NO_TAKES_TODAY` and no second Telegram message is sent.
- Updated `openclaw-scripts/README.md` to describe the new behavior.

### Two-message flow
The morning-sports cron job now sends two Telegram messages on most days:
1. `*Morning Sports Digest -- <date>*` -- the existing emoji-organized digest with Star of the Night.
2. `*Today's Takes -- <date>*` -- 3-5 original-post candidates in Markdown, each with `Trigger / Angle / Hook` lines.

On thin news days only the first message is sent (takes returns `NO_TAKES_TODAY` and the second send is skipped).

## Why
Steve named the gap in our scoping conversation:

> "It's more about looking opportunities to reply to big X influencers or keep posting content at viable rate to stay relevant and gain a following to my substack work."

Reply candidates (Bundle #1) serve the first half of that goal. Original-post candidates serve the second half -- a stream of writing prompts so Steve can ship at a viable rate without having to hunt for what to write about. The morning digest already does the expensive work (fetches and bundles yesterday's sport-tagged tweets); reusing that bundle for a takes call is essentially zero marginal cost.

## Cost
- One additional Claude Haiku call per day (~$0.001-0.005). Trivial.
- Telegram bot API is free.
- No new X API spend (reuses the existing morning-digest bundle).

## Earnest's required actions on Mac Mini (in order)
1. `cd ~/autopilot && git pull` to pick up the morning-sports-digest changes.
2. **No cron changes needed** -- the existing 6:00 AM ET morning-sports-digest job picks up the new behavior automatically on its next firing.
3. After tomorrow's 6 AM run, confirm two Telegram messages arrive (or one + a log line `No takes today` if news was thin).

## Verification (after Earnest pulls)
- Dry-run check on Mac Mini: `bash ~/autopilot/openclaw-scripts/morning-sports-digest.sh --dry-run` prints both the digest section and (if takes are non-empty) a `--- DRY RUN TAKES ---` block. No Telegram traffic in dry-run.
- Live run after Earnest's pull: morning-digest cron job at 6:00 AM ET on the next firing should produce two Telegram messages.

## Rollback / Kill Switch
- `git revert` the autopilot commit. The `morning-sports-digest.R` script falls back to its prior single-message digest behavior. The existing cron job needs no change either way.

## Follow-ups
- [ ] After 2-3 weeks of Takes messages, get Steve's read on whether the candidates are actually inspiring posts. If hit rate is low, tune the prompt (likely candidates: stricter angle filtering, more aggressive `NO_TAKES_TODAY` threshold).
- [ ] Future iteration: fold in unposted tier-1 x-monitor surfacings from `data/x-monitor-surfacings.jsonl` (Bundle #3 data) as an additional source signal -- a tier-1A handle's tweet that Steve didn't reply to is a strong original-post signal. Defer until Bundle #3 has 2+ weeks of data.
- [ ] Consider adding light feedback (single button: "wrote it" / no button) on takes messages if signal-vs-noise becomes a question. Not in scope for Bundle #4 v1.

---

### Bundle #3 Bug Fix: Refactor Feedback Loop to Log-Only Contract

## Type
- [x] Bug fix
- [x] Configuration change

## Summary
Bundle #3 as originally shipped (commit `0e50ca1`) had `x-feedback-send.R` call Telegram directly and `x-feedback-poll.R` poll Telegram's `getUpdates` endpoint. Both behaviors conflict with OpenClaw, which is the bot token's primary owner and update router. Symptoms per Earnest's incident report:

> **Bug 1**: `x-feedback-send.sh` messages not visible to Steve. Bot is `merrittocracybot` (same token). API returns `ok:true` + valid `message_id` (4324, 4346 confirmed). Steve cannot see them in Telegram. Suspected cause: OpenClaw may be suppressing/deleting outbound bot messages it did not originate, or routing them somewhere else.
>
> **Bug 2**: `x-feedback-poll.sh` always returns 0 updates. OpenClaw consumes `getUpdates` before the poller script can read them (classic two-poller conflict). `callback_query` events for inline button clicks will never reach the R poller.

Bug 2 is unambiguously a two-poller conflict -- Telegram's `getUpdates` returns each update exactly once and OpenClaw was always going to win that race. Bug 1's root cause is less certain but the symptom is consistent with OpenClaw owning the bot's outbound channel.

Fix: refactor both scripts to **log-only contracts**. All Telegram I/O (sends, callback reception, ACKs) is performed by Earnest through OpenClaw's routing layer. The autopilot scripts only handle JSONL log persistence -- a clean separation of concerns that respects OpenClaw's ownership of the bot.

## Details

### Files renamed and rescoped (autopilot)
- `openclaw-scripts/x-feedback-send.R` -> `openclaw-scripts/x-surfacing-log.R`
- `openclaw-scripts/x-feedback-send.sh` -> `openclaw-scripts/x-surfacing-log.sh`
- `openclaw-scripts/x-feedback-poll.R` -> `openclaw-scripts/x-feedback-log.R`
- `openclaw-scripts/x-feedback-poll.sh` -> `openclaw-scripts/x-feedback-log.sh`

### Behavior changes
- Removed all `httr2` Telegram API calls from both scripts.
- Removed all references to `TELEGRAM_BOT_TOKEN` (no longer needed -- the log scripts don't talk to Telegram).
- Removed the poller offset state file `data/x-feedback-poller-state.json` (no more polling, no offset to track). The `.gitignore` entry for it was removed.

### JSONL log shapes are unchanged
The contents of `data/x-monitor-surfacings.jsonl` and `data/x-monitor-feedback.jsonl` keep the same schema as the original Bundle #3 design. Only the writer changes.

## New contract for Earnest's drafting step

### When sending a candidate to Telegram
1. Build the inline keyboard. `callback_data` for each button MUST be formatted exactly `fb:{tweet_id}:{bucket_code}` where bucket_code is one of:
   - `s` -- skip
   - `c` -- posted_clean (as-is)
   - `e` -- posted_edited
2. Send the message + keyboard via OpenClaw's send-with-inline-buttons primitive.
3. After successful send, pipe this JSON to `x-surfacing-log.sh` on stdin:
   ```
   {
     "candidate":  { ...x-monitor candidate JSON... },
     "draft":      "...drafted reply text...",
     "message_id": <telegram-returned message_id>
   }
   ```

### When a callback_query arrives (button click)
1. OpenClaw routes the `callback_query` to Earnest.
2. Parse `callback_data`: it will be `fb:{tweet_id}:{bucket_code}`.
3. ACK the callback via OpenClaw's `answerCallbackQuery` primitive with a short toast (e.g., "Logged: Skip").
4. Pipe this JSON to `x-feedback-log.sh` on stdin:
   ```
   {
     "tweet_id":    "1234567890123456789",
     "bucket_code": "s",
     "message_id":  7891,
     "chat_id":     "8676616323",
     "from_user":   "merrittocratic",
     "callback_id": "<telegram callback_query id>",
     "update_id":   <telegram update_id>
   }
   ```
   The script maps `bucket_code` to the canonical bucket label (`skip`/`posted_clean`/`posted_edited`) automatically.

## Earnest's required actions on Mac Mini (in order)
1. `cd ~/autopilot && git pull` to pick up the renamed scripts.
2. **Delete the `x-feedback-poll` cron job** -- no longer used. (Created during the original Bundle #3 deploy.)
3. **Update the drafting step** to use OpenClaw's send-with-inline-buttons primitive instead of bypassing to Telegram directly. Callback data format as specified above.
4. **Implement a `callback_query` handler** in Earnest's session that ACKs via OpenClaw and pipes feedback JSON to `x-feedback-log.sh`.
5. **Delete stale runtime artifacts** if they exist:
   - `data/x-feedback-poller-state.json` -- no longer used.
   - Any partial entries in `data/x-monitor-surfacings.jsonl` or `data/x-monitor-feedback.jsonl` from the broken initial deploy (Earnest's call on whether they're worth keeping for debugging vs. truncating).

## Verification
- Trigger a candidate send through Earnest's flow.
  - Telegram message appears in Steve's chat with three inline buttons.
  - New line in `data/x-monitor-surfacings.jsonl` matching the candidate.
- Click each of the three buttons.
  - Each click produces an ACK toast in Telegram within ~1 second (OpenClaw handles this; no cron latency anymore).
  - One new line per click in `data/x-monitor-feedback.jsonl`.

## Rollback / Kill Switch
- There is no "revert to old behavior" path because the old behavior was broken. To fully disable feedback capture without reverting, Earnest can simply stop calling the log scripts -- candidate sends still work, just nothing gets recorded.

## Follow-ups
- [ ] If Bug 1 turns out to NOT be OpenClaw filtering (e.g., wrong chat_id, malformed reply_markup), document the actual root cause in a new entry. The log-only refactor is correct regardless -- two pollers fighting for `getUpdates` was unambiguously a bug.
- [ ] Update the analysis-script follow-up from the original Bundle #3 entry to point at the new script names if/when that work happens.
